{
  "mcpServers": {
    "gh-pr-mcp": {
      "command": "uv",
      "args": ["run", "gh-pr-mcp"],
      "description": "GitHub PR and review helpers (this worktree — restart Claude Code to hot-reload)"
    }
  }
}
