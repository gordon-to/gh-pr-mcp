{
  "mcpServers": {
    "gh-mcp": {
      "command": "uv",
      "args": ["run", "gh-mcp"],
      "description": "git + GitHub CLI tools (this worktree — restart Claude Code to hot-reload)"
    }
  }
}
